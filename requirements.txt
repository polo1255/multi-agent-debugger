 --- 1. Orchestration & State Management ---
langgraph            จัดการ Cyclic Graph และ Workflow [cite: 6, 27]
langchain            Framework หลักสำหรับเชื่อมต่อ LLMs [cite: 125]

 --- 2. LLMs (The Brain) ---
langchain-anthropic  สำหรับเชื่อมต่อ Claude 3.5 Sonnet [cite: 10, 38]
langchain-openai     สำหรับเชื่อมต่อ GPT-4o [cite: 10, 38]
openai               สำหรับเชื่อมต่อ DeepSeek-Coder-V2 (ผ่าน OpenAI-compatible API) [cite: 11, 35]

 --- 3. Sandbox & Security ---
docker               Docker SDK สำหรับ Python เพื่อจัดการ Sandbox [cite: 13, 43]

 --- 4. Persistence & Knowledge Layer ---
chromadb             Vector Database สำหรับเก็บประสบการณ์การแก้บั๊ก [cite: 18, 46]
psycopg2-binary      สำหรับเชื่อมต่อกับ PostgreSQL เก็บสถิติ [cite: 21, 47]

 --- 5. Utilities ---
python-dotenv       สำหรับโหลดค่า API Key จากไฟล์ .env